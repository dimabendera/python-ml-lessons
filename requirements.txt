# Потрібно лише для практичних зошитів (practice.ipynb).
# Лекції, тести й домашні завдання — звичайні HTML-сторінки, їм нічого не треба.

# --- потрібно всюди ---
numpy
pandas
matplotlib
scikit-learn
scipy
jupyter

# --- курс Python ---
requests          # теми 27-29; ходять лише на 127.0.0.1, підняти локальний сервер
beautifulsoup4    # тема 29
pytest            # тема про тести
regex             # ширші класи символів, ніж у вбудованому re

# --- «Машинне навчання» і «Компʼютерний зір» ---
torch             # 35 тем; імпортується без запобіжника, без нього вони не працюють
torchvision       # 19 тем; архітектури беруться з weights=None, ваги не качаються
opencv-python     # cv2, 11 тем
pillow            # PIL

# --- «Обробка природної мови» ---
pymorphy3         # морфологія української, тема 03 і далі
pymorphy3-dicts-uk # словники dict_uk; без них pymorphy3 української не знає
tokenizers        # BPE і WordPiece, тема 02
rapidfuzz         # відстані між рядками
nltk              # тема 19: звірка власної реалізації BLEU з еталонною; завантажень не потребує
seqeval           # тема 32: F1 по сутностях; зошит звіряє з ним власну реалізацію
faiss-cpu         # тема 36: векторний індекс; імпортується як faiss, мережі не потребує

# --- необовʼязкове: зошит сам пропустить розділ, якщо цього немає ---
# pytesseract     # CV, тема 33 · потрібен ще системний рушій tesseract з мовою ukr
# ultralytics     # CV, детекція
# filterpy        # CV, супровід обʼєктів
# shap            # ML, пояснення моделей
# rank-bm25       # NLP, тема 09 · у нас BM25 усе одно написаний вручну

# ⚠️ Курс «Обробка природної мови» бере корпус із системи, а не з мережі:
# /usr/share/locale/uk/LC_MESSAGES/*.mo. Це не пакет Python — це українські
# переклади встановлених програм. Без них більшість зошитів курсу не виконається.
# Перевірка: ls /usr/share/locale/uk/LC_MESSAGES/*.mo | wc -l
