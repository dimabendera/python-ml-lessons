<h1>Підсумок<a href="#summary" title="Постійне посилання на цей заголовок"></a></h1>
<ol>
<li><p>Дані часто потрібно зберігати на диск для подальшої сесії Python або для читання іншими програмами.</p></li>
<li><p>Дані, створені іншими програмами, можливо, доведеться читати за допомогою Python.</p></li>
<li><p>Python має функції для читання та запису даних у кількох стандартних форматах: txt, csv, pickle та hdf5.</p></li>
</ol>


<h1>Завдання<a href="#problems" title="Постійне посилання на цей заголовок"></a></h1>
<ol>
<li><p>Створіть список і збережіть його у текстовому файлі так, щоб кожен елемент списку займав один рядок.</p></li>
<li><p>Збережіть той самий список із завдання 1 у файл CSV.</p></li>
<li><p>Створіть двовимірний масив <code><span>numpy</span></code>, збережіть його у файл CSV та прочитайте назад у двовимірний масив.</p></li>
<li><p>Збережіть той самий масив із завдання 2 у файл pickle та завантажте його назад.</p></li>
<li><p>Створіть словник і збережіть його у файл JSON.</p></li>
<li><p>Створіть одновимірний масив <code><span>numpy</span></code>, збережіть його у файл JSON з ключем під назвою 'data'. Потім завантажте його назад.</p></li>
</ol>
